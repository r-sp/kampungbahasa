{
  "name": "kampungbahasa",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.x",
    "pnpm": "9.x"
  },
  "packageManager": "pnpm@9.12.1",
  "scripts": {
    "dev": "turbo watch dev",
    "build": "turbo run build",
    "format": "turbo run format --cache-dir=.turbo/.cache/.prettiercache",
    "lint": "turbo run lint --cache-dir=.turbo/.cache/.eslintcache",
    "clean": "turbo run clean --no-cache"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "prettier-plugin-tailwindcss": "^0.6.8",
    "turbo": "2.1.3"
  }
}
